<!DOCTYPE html>
<html>
  <head>
    <title>AxisApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="header">
      <div class="header__bar row">
        <h1 class="grid-6"><a class="thefool" href="/">The Fool</a></h1>
        <% if user_signed_in? %>
          <div class="user_nav grid-6">
            <%= link_to "目標なし", destroy_user_session_path, data: { turbo_method: :delete }, class: "out" %>
            <%= link_to "設定する", new_vision_path, class: "post" %>
          </div>
        <% else %>
          <div class="grid-6">
            <%= link_to "ログイン", new_user_session_path, class: "post" %>
            <%= link_to "新規登録", new_user_registration_path, class: "post" %>
          </div>
        <% end %>
    </header>
    <%= yield %>
    <footer>
      <p>
        Visionary
      </p>
    </footer>
  </body>
</html>
